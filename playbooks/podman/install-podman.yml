---
# Example playbook: Install Podman only
# Minimal playbook for Podman installation

- name: Install Podman
  hosts: all
  become: true

  collections:
    - kode3tech.devtools

  vars:
    podman_rootless_users:
      - "{{ ansible_user }}"

  roles:
    - podman
